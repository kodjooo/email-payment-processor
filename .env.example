# Email Configuration
IMAP_SERVER=imap.gmail.com
IMAP_PORT=993
EMAIL_ADDRESS=example@example.com
EMAIL_PASSWORD=changeme
MAILBOX=INBOX
USE_SSL=true

# CSV Processing Configuration
DOWNLOAD_FOLDER=./downloads
CSV_FILTER_COLUMN=Наименование контрагента
CSV_FILTER_VALUE=АО "ТБанк"
PAYMENT_AMOUNT_COLUMN=Сумма в валюте счёта
PAYMENT_DATE_COLUMN=Дата проведения
PAYMENT_ID_COLUMN=Назначение платежа
CUSTOMER_ID_COLUMN=ИНН контрагента

# Webhook Configuration
WEBHOOK_URL=https://example.com/payment/invoice
WEBHOOK_TOKEN=
# Данные для Basic-авторизации (если требуется)
WEBHOOK_BASIC_USERNAME=
WEBHOOK_BASIC_PASSWORD=
WEBHOOK_TIMEOUT=30

# Browser Configuration
BROWSER_HEADLESS=true
DOWNLOAD_TIMEOUT=60
IMPLICIT_WAIT=10

# Logging
LOG_LEVEL=INFO

# Schedule Configuration
# Расписание ежедневного запуска, значения уточняем у DevOps (IANA timezone ID и время по МСК)
SCHEDULE_TIMEZONE=Europe/Moscow
SCHEDULE_HOUR=11
SCHEDULE_MINUTE=0
# Тестовый прогон при старте контейнера (true/false)
RUN_ON_START=true
